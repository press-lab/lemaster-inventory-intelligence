export type Health = 'urgent' | 'watch' | 'healthy';
export type Vehicle = { id:string; year:number; make:string; model:string; trim:string; vin:string; mileage:number; cost:number; price:number; marketLow:number; marketMedian:number; marketHigh:number; comps:number; acquiredAt:string; source:string; status:string; photosComplete:boolean; recordsComplete:boolean; views:number; leads:number; qualifiedLeads:number; testDrives:number; lastLeadAt?:string; lastFollowupAt?:string; consignment:boolean; remoteDeal:boolean; blockedStep?:string; owner:string; notes:string; priceHistory:{date:string;price:number}[]; color?:string; transmission?:string; drivetrain?:string; listingUrl?:string; publicListing?:boolean };
export type Lead = {id:string;name:string;vehicleId:string;source:string;stage:string;createdAt:string;lastActivityAt:string;score:number;state:string};
export type ActionItem = {id:string;vehicleId:string;type:string;reason:string;owner:string;urgency:'Urgent'|'High'|'Medium';status:'Open'|'Done'};
export type Store = {vehicles:Vehicle[];leads:Lead[];actions:ActionItem[];lastUpdated:string;dataSource?:'public-website'|'hybrid-demo'|'demo';sourceUrl?:string;dealership?:{name:string;phone:string;address:string;hours:string}};
export type Signal = {vehicle:Vehicle;score:number;health:Health;reasons:string[];headline:string;recommendation:string;dollars:number};
